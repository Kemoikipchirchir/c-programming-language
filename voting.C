Author =Edwin kipchirchir kemoi 
Date= 13/6/2024
Description=program to check if user is eligible vote    
// eligibility to vote 
#include <stdio.h>
int main() { 
int age; //variable to store the users' age
char nationality,kenyan; 
  
printf("please enter the value of age:\n");
  scanf("%d",&age); //get the user's age input
  
printf("please the nationality:\n");
  scanf("%char",&nationality); //get the users' nationality input
  
   //check if the user meeets the voting eligibility
if("age=18")
if("nationality=kenyan"){
printf("you are eligible to vote" );
}
else{
printf("you cannot vote" );   
}
